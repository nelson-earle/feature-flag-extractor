{
    "$schema": "http://json-schema.org/schema",
    "type": "object",
    "cli": "nx",
    "title": "Extract Launch Darkly feature flag IDs from an Angular application",
    "description": "Custom executor for my specific task",
    "required": ["tsConfig"],
    "properties": {
        "tsConfig": {
            "type": "string",
            "description": "The path of the tsConfig to use for parsing TypeScript files"
        },
        "logLevel": {
            "type": "string",
            "description": "The maximum log level",
            "enum": ["error", "warn", "info", "debug", "debug2"]
        },
        "locations": {
            "type": "boolean",
            "description": "Whether to print the locations of uses of each feature flag"
        },
        "output": {
            "type": "string",
            "description": "Write the output to the given path"
        },
        "json": {
            "type": "boolean",
            "description": "Whether to print the output as JSON"
        }
    }
}
